name: CMake on multiple platforms

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        build_type: [Release]
        c_compiler: [gcc, cl]
        include:
          - os: windows-latest
            c_compiler: cl
            cpp_compiler: cl
          - os: ubuntu-latest
            c_compiler: gcc
            cpp_compiler: g++
        exclude:
          - os: windows-latest
            c_compiler: gcc
          - os: windows-latest
            c_compiler: clang
          - os: ubuntu-latest
            c_compiler: cl

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies on Linux
      if: matrix.os == 'ubuntu-latest'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential libx11-dev mesa-common-dev libglu1-mesa-dev libxi-dev libxmu-dev libxmu-headers
        sudo apt-get install -y libncurses-dev libtirpc-dev
        sudo apt-get install -y ninja-build

    - name: Install PowerShell on Linux
      if: matrix.os == 'ubuntu-latest'
      shell: bash
      run: |
        sudo apt-get update
        sudo apt-get install -y powershell

    - name: Set reusable strings
      id: strings
      shell: pwsh
      run: |
        $buildOutputDir = "${{ github.workspace }}/build"
        echo "build-output-dir=$buildOutputDir" >> $env:GITHUB_OUTPUT

    - name: Cache vcpkg
      uses: actions/cache@v3
      with:
        path: vcpkg
        key: vcpkg-${{ runner.os }}-${{ github.sha }}
        restore-keys: |
          vcpkg-${{ runner.os }}-

    - name: Cache vcpkg_installed
      uses: actions/cache@v3
      with:
        path: ${{ steps.strings.outputs.build-output-dir }}/vcpkg_installed
        key: vcpkg_installed-${{ runner.os }}-${{ github.sha }}
        restore-keys: |
          vcpkg_installed-${{ runner.os }}-

    - name: Remove existing vcpkg directory if not cached
      if: steps.cache-vcpkg.outputs.cache-hit != 'true'
      shell: pwsh
      run: |
        if (Test-Path -Path "vcpkg") {
            Remove-Item -Recurse -Force vcpkg
        }

    - name: Remove existing vcpkg_installed directory if not cached
      if: steps.cache-vcpkg_installed.outputs.cache-hit != 'true'
      shell: pwsh
      run: |
        if (Test-Path -Path "${{ steps.strings.outputs.build-output-dir }}/vcpkg_installed") {
          Remove-Item -Recurse -Force "${{ steps.strings.outputs.build-output-dir }}/vcpkg_installed"
        }

    - name: Bootstrap vcpkg on Windows
      if: matrix.os == 'windows-latest'
      shell: pwsh
      run: |
        if (-not (Test-Path -Path "vcpkg")) {
            git clone https://github.com/microsoft/vcpkg.git vcpkg
            cd vcpkg
            .\bootstrap-vcpkg.bat -disableMetrics
            .\vcpkg integrate install
        }

    - name: Bootstrap vcpkg on Linux
      if: matrix.os == 'ubuntu-latest'
      shell: pwsh
      run: |
        if (-not (Test-Path -Path "vcpkg")) {
          git clone https://github.com/microsoft/vcpkg.git vcpkg
          cd vcpkg
          ./bootstrap-vcpkg.sh -disableMetrics
          ./vcpkg integrate install
        }

    - name: Set VCPKG_ROOT environment variable
      shell: pwsh
      run: |
        echo "VCPKG_ROOT=${{ github.workspace }}/vcpkg" >> $env:GITHUB_ENV
        echo "VCPKG_FEATURE_FLAGS=versions" >> $env:GITHUB_ENV

    - name: Test Tools Windows
      if: matrix.os == 'windows-latest'
      run: |
        echo "Testing tools on Windows..."
        if (-not (Test-Path -Path "${{ env.VCPKG_ROOT }}")) {
          echo "VCPKG_ROOT is not set or the path does not exist"
        } else {
        echo "VCPKG_ROOT: ${{ env.VCPKG_ROOT }}"
        }
        echo "CMake version:"
        cmake --version
        if ($LASTEXITCODE -ne 0) { exit 1 }
        echo "MSVC Compiler version:"
        cl /?
        if ($LASTEXITCODE -ne 0) { exit 1 }
        echo "Ninja version:"
        ninja --version
        if ($LASTEXITCODE -ne 0) { exit 1 }

    - name: Test Tools Linux
      if: matrix.os == 'ubuntu-latest'
      shell: bash
      run: |
        echo "Testing tools on Linux..."
        if [ -z "${{ env.VCPKG_ROOT }}" ]; then
          echo "VCPKG_ROOT is not set"
        else
          echo "VCPKG_ROOT: ${{ env.VCPKG_ROOT }}"
        fi
        echo "CMake version:"
        cmake --version || exit 1
        echo "GCC version:"
        gcc --version || exit 1
        echo "G++ version:"
        g++ --version || exit 1
        echo "Ninja version:"
        ninja --version || exit 1


    - name: Configure CMake With Vcpkg Toolchain on Windows
      if: matrix.os == 'windows-latest'
      run: |
        echo "Configuring CMake with vcpkg toolchain..."
        cmake -B ${{ steps.strings.outputs.build-output-dir }} `
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} `
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} `
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} `
        -DCMAKE_TOOLCHAIN_FILE=${{ env.VCPKG_ROOT }}/scripts/buildsystems/vcpkg.cmake `
        -S ${{ github.workspace }} -G Ninja

    - name: Configure CMake With Vcpkg Toolchain on Linux
      if: matrix.os == 'ubuntu-latest'
      run: |
        echo "Configuring CMake with vcpkg toolchain..."
        cmake -B ${{ steps.strings.outputs.build-output-dir }} \
        -DCMAKE_CXX_COMPILER=${{ matrix.cpp_compiler }} \
        -DCMAKE_C_COMPILER=${{ matrix.c_compiler }} \
        -DCMAKE_BUILD_TYPE=${{ matrix.build_type }} \
        -DCMAKE_TOOLCHAIN_FILE=${{ env.VCPKG_ROOT }}/scripts/buildsystems/vcpkg.cmake \
        -S ${{ github.workspace }} -G Ninja

    - name: Build Windows
      if: matrix.os == 'windows-latest'
      run: |
        echo "Building project..."
        cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Build Linux
      if: matrix.os == 'ubuntu-latest'
      shell: bash
      run: |
        echo "Building project..."
        cmake --build ${{ steps.strings.outputs.build-output-dir }} --config ${{ matrix.build_type }}

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}/tests
      run: |
        echo "Running tests..."
        ctest --build-config ${{ matrix.build_type }}
      continue-on-error: true